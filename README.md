# Veganistan Angular application

## An angular driven frontend application for Veganistan

Nothing to see yet.

We want to use AngularJS as a node module so that we can ``require()`` it
through ``Browserify``::

    npm install -g napa

